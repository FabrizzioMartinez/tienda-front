export interface Marca {
  marcaID: number;
  nombre: string;
  activo: boolean;
}
// src/app/core/models/marca.model.ts

export interface MarcaDto {
  marcaID: number;
  nombre: string;
  activo:boolean;
}